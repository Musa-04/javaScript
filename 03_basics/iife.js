(function chai(){
    console.log(`DB is connected`);
    
})();
// This is an IIFE (Immediately Invoked Function Expression) that logs a message to the console when the script is executed.

(function chai(){
    console.log(`DB is connected successfully`);
    
})(); // This is an IIFE (Immediately Invoked Function Expression) that logs a message to the console when the script is executed.
// This is an IIFE (Immediately Invoked Function Expression) that logs a message to the

((name)=>{
    console.log(`DB is connected successfully Two ${name}`);
    
})('musa'); // This is an IIFE (Immediately Invoked Function Expression) that logs a message to the console when the script is executed.
